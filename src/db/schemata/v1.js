/* eslint-disable import/prefer-default-export */

export const iterations = [
  "++id",
  "startedAt",
  "type",
  "numOfIteration",
  "totalTimeInMillis",
].join(",");

export const tasks = [
  "++id",
  "title",
  "createedAt",
  "completedAt",
].join(",");

/* eslint-enable */
